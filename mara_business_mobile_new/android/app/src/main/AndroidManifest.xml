<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mara_business.app">

    <!-- ✅ Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      allowBackup was true, which put SharedPreferences — where the auth token is
      kept — into Android's backup: extractable with adb backup on many devices
      without root, and synced to the user's Google Drive. Paired with tokens that
      never expired server-side, one recovered backup meant permanent access to
      that account. Tokens now expire after 90 days (config/sanctum.php) and this
      app no longer offers its data for backup. Nothing here is worth restoring
      across devices: the session is the only local state, and it is meant to be
      re-established by signing in.
    -->
    <application
        android:label="Mara Business"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- 👇 LAUNCHER INTENT FILTER (MUST BE HERE) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 👇 DEEP LINK (for OAuth) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mara" android:host="login" />
                <data android:scheme="https" android:host="afrobridgeinnov.com" />
            </intent-filter>

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>